import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  title: {
    fontSize: 24,
  },
  container: {
    flex: 1,
    alignItems: "center",
    justifyContent: "center",
  },
  searchBar: {
    borderWidth: 1,
    width: "80%",
    height: 40,
    padding: 10,
    marginTop: 16,
    borderRadius: 8,
  },
});
