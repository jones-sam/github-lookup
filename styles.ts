import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  title: {
    fontSize: 28,
  },
  container: {
    flex: 1,
    alignItems: "center",
    justifyContent: "center",
  },
  searchBar: {
    borderWidth: 1,
    width: "80%",
    height: 40,
    padding: 10,
    marginTop: 16,
    marginBottom: 8,
    borderRadius: 8,
  },
  followerText: {
    fontSize: 20,
    padding: 4,
    marginLeft: 8,
  },
});
